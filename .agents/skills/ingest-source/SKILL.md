---
name: ingest-source
description: Ingest a new source into this event repo (pasted chat thread, email, call transcript, link, team-written document, PDF or image, or "process the inbox"). Files it verbatim under sources/, registers it, extracts every fact, decision, action and question, routes each to the file that owns it, updates STATUS.md, logs, and reports.
---

Read and follow `workflows/ingest-source.md` in this repo, step by step, for the material the
user has given you (or for every file in `inbox/` if asked to process the inbox). Do not skip
step 1. Finish with the report in the shape the workflow specifies.
